import { Navbar } from "react-bootstrap"

const Nav = () => {
    return (
            <Navbar>
                <a href = "#">Nosotros</a>
                <a href = "#">Tratamientos</a>
                <a href = "#">Contacto</a>
            </Navbar>
    )
}

export default Nav